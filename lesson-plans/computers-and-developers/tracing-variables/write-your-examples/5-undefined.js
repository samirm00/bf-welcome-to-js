'use strict';

/* Undefined

TODO: create a few variables(undefined):
- you need to add comments to show where the variable been
declared, initialized, read , reassigned,...
*/

// declare a variable message  and initialize it to the value undefined
const message = undefined;

// declare a variable info
let info;

// read the variable message
console.log(message);

// read the variable info,
console.log(info);
// Note : the value of info is undefined by default
// if we didn't initialize it
